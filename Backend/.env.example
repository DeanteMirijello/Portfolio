PORT=3001
# Set to your Astro dev origin or deployed site
ALLOW_ORIGIN=http://localhost:4321
# Optionally allow multiple origins (comma-separated)
# ALLOW_ORIGINS=http://localhost:4321,http://127.0.0.1:4321

# Auth0 (required for protected routes)
# Your tenant domain (without protocol)
AUTH0_DOMAIN=your-tenant.us.auth0.com
# The API Identifier (audience) you configure in Auth0 > APIs
AUTH0_AUDIENCE=your-api-identifier
# Optional: mark specific emails as admins (comma-separated)
# Example: AUTH0_ALLOWED_ADMIN_EMAILS=deante.mirijello@gmail.com,teacher@example.com
AUTH0_ALLOWED_ADMIN_EMAILS=
# Optional: where roles are injected in the token/user claims
AUTH0_ROLES_CLAIM=https://your.app/roles